<h1><%= link_to(@technology, technology_path(@technology)) %> >> <%= @category %></h1>

<ul>
  <% @repositories.each do |repository| %>
    <li><%= link_to repository, repository_path(@technology, @category, repository) %></li>
  <% end %>
</ul>
